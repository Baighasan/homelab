- name: Lock down SSH password authentication
  hosts: laptops
  gather_facts: true
  become: true
  serial: 1

  roles:
    - role: security
      vars:
        security_ssh_password_authentication: "no"
      tags: [security, lockdown]
